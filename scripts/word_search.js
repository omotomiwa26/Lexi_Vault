function searchWord() {
    // Get the input value
    var word = document.getElementById("wordInput").value;
    // Simulate searching (replace with actual search functionality)
    var result = "Definition of " + word + " goes here.";
    // Display the result
    document.getElementById("searchResult").innerText = result;
}